# Example environment variables for MediaSet development

# Frontend (Remix) Configuration - Runtime variables (set via environment at startup)
# Server-side API URL (used by Remix server for API calls, can use Docker container names)
# apiUrl=http://api:5000
# Client-side API URL (used by browser for images/assets, must be accessible from host)
clientApiUrl=http://localhost:5000
NODE_ENV=development
REMIX_DEV_HTTP_ORIGIN=http://localhost:3000

# Optional: Show detailed error information in the browser (development only)
SHOW_ERROR_DETAILS=false

# API (.NET) Configuration  
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:5000;https://+:5001
# MediaSetDatabase__ConnectionString=mongodb://mongodb:27017
# MediaSetDatabase__DatabaseName=MediaSet

# Open Library Configuration (for book barcode lookup)
# OpenLibraryConfiguration__BaseUrl=https://openlibrary.org/
# OpenLibraryConfiguration__Timeout=30
# OpenLibraryConfiguration__ContactEmail=your-email@example.com

# TMDB Configuration (for movie barcode lookup)
# Get your bearer token at: https://www.themoviedb.org/settings/api
# See TMDB_SETUP.md for detailed setup instructions
# TMDB_BEARER_TOKEN=your_tmdb_bearer_token_here

# UPCitemdb Configuration (for barcode lookup)
# Get your API key at: https://www.upcitemdb.com/api (optional, for better rate limits)
# UPCITEMDB_API_KEY=your_upcitemdb_api_key_here

# GiantBomb Configuration (for game barcode lookup)
# Request an API key at: https://www.giantbomb.com/api/
# See GIANTBOMB_SETUP.md for detailed setup instructions
# GIANTBOMB_API_KEY=your_giantbomb_api_key_here
# Optional override; defaults to https://www.giantbomb.com/api/
# GIANTBOMB_BASE_URL=https://www.giantbomb.com/api/

# MongoDB Configuration
MONGO_INITDB_DATABASE=MediaSet